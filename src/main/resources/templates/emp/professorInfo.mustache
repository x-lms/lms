<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>학습관리시스템 - 교수 상세</title>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <link rel="stylesheet" href="/css/common.css">
    <link rel="stylesheet" href="/css/layout.css">
    <link rel="stylesheet" href="/css/stylejm.css">
</head>
<body>
    <!-- include -->
    {{>layouts/header}}
    <div class="container">
        {{>layouts/navEmp}}
        <div class="main-content">
            <div class="page-header">
                <h2 class="page-title">교수 정보</h2>
            </div>
            
            <div class="professor-detail-container">
                <div class="professor-detail-header">
                    <h3 class="section-title">기본 정보</h3>
                </div>
				{{#professor}}
	                <div class="professor-detail-content">
	                    <div class="professor-photo-section">
	                        <div class="professor-photo-large">
	                        	{{#prfImg}}
	                            <img src="/uploads/{{prfImg}}">
								{{/prfImg}}
								{{^prfPhoto}}
									<img src="/uploads/default.png">
								{{/prfPhoto}}
						</div>
	                    </div>
	
	                    <div class="info-grid">
	                        <div class="info-grid-row">
	                            <span class="info-grid-label">이름</span>
	                            <span class="info-grid-value">{{prfName}}</span>
	                        </div>
	                        <div class="info-grid-row">
	                            <span class="info-grid-label">이메일</span>
	                            <span class="info-grid-value">{{prfEmail}}</span>
	                        </div>
	                        <div class="info-grid-row">
	                            <span class="info-grid-label">학과</span>
	                            <span class="info-grid-value">{{deptName}}</span>
	                        </div>
	                        <div class="info-grid-row">
	                            <span class="info-grid-label">휴대폰번호</span>
	                            <span class="info-grid-value">{{#prfPhone}}{{prfPhone}}{{/prfPhone}}</span>
	                        </div>
	                        <div class="info-grid-row">
	                            <span class="info-grid-label">생년월일</span>
	                            <span class="info-grid-value">{{#prfBirth}}{{prfBirth}}{{/prfBirth}}</span>
	                        </div>
	                    </div>
	                </div>

	                <div class="professor-detail-actions">
	                    <a href="/emp/professorList" class="btn-secondary">목록</a>
	                    <div class="action-right">
	                        <a href="/emp/modifyProfessor?prfNo={{prfNo}}" class="btn-edit">수정</a>
	                        <button type="button" class="btn-delete" onclick="deleteProfessor({{prfNo}})">삭제</button>
	                    </div>
	                </div>
				{{/professor}}
            </div>
        </div>
    </div>
    {{>layouts/footer}}

    <script>
        function deleteProfessor(prfNo) {
            if(confirm('이 교수 정보를 삭제하시겠습니까?')) {
				console.log(prfNo);
                // 삭제 로직
                location.href = '/emp/deleteProfessor?prfNo=' + prfNo;
            }
        }
    </script>
</body>
</html>