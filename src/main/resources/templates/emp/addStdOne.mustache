<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>학습관리시스템 - 학생등록</title>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <link rel="stylesheet" href="/css/common.css">
    <link rel="stylesheet" href="/css/layout.css">
    <link rel="stylesheet" href="/css/stylejm.css">
</head>
<body>
    <!-- include -->
    {{>layouts/header}}
    <div class="container">
        {{>layouts/navEmp}}
        <div class="main-content">
            <div class="page-header">
                <h2 class="page-title">학생등록</h2>
            </div>
            
            <div class="notice-form-container">
                <form id="studentForm" class="notice-form" action="{{springMacroRequestContext.request.contextPath}}/emp/addStdOne" method="post">
                    <div class="form-row">
                        <label class="form-label required">입학/편입년도</label>
                        <input type="number" name="year" class="form-input" placeholder="년도를 입력하세요 ex)2025" required>
                    </div>
                    <div class="form-row">
                        <label class="form-label required">이름</label>
                        <input type="text" name="studentName" class="form-input" placeholder="학생 이름을 입력하세요" required>
                    </div>

					<div class="form-row-split">
						<div class="form-col">
							<label class="form-label required">학과</label>
							<select name="deptNo" class="form-select" required>
								<option value="">학과를 선택하세요</option>
								{{#deptList}}
									<option value="{{deptNo}}">{{deptName}}</option> 
								{{/deptList}}
							</select>
						</div>
	
						<div class="form-col">
							<label class="form-label required">학적상태</label>
							<select name="studentState" class="form-select" required>
								{{#stateList}}
									<option value="{{.}}">{{.}}</option>
								{{/stateList}}
							</select>
						</div>
					</div>

					<div class="form-actions">
                        <a href="{{springMacroRequestContext.request.contextPath}}/emp/studentList" class="btn-secondary">취소</a>
                        <button type="submit" class="btn-primary">등록</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
    {{>layouts/footer}}
</body>
</html>