<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>{{project.projectName}} 과제 제출</title>
    <link rel="stylesheet" href="/css/common.css">
    <link rel="stylesheet" href="/css/layout.css">
    <link rel="stylesheet" href="/css/studentProjectResultOne.css">
</head>
<body>

{{> layouts/header }}

<div class="container">
    {{> layouts/navStudent }}

    <div class="main-content">

        {{#msg}}
        <div class="alert-success">{{msg}}</div>
        {{/msg}}

        
        <div class="detail-box">
            <h2 class="detail-title">{{project.projectName}}</h2>

            <div class="course-info">
                <div class="info-item">
                    <label>마감일:</label>
                    <span class="deadline">{{project.projectDeadline}}</span>
                </div>

                {{#project.projectContents}}
                <div class="info-item">
                    <label>설명:</label>
                    <span>{{project.projectContents}}</span>
                </div>
                {{/project.projectContents}}
            </div>

            
            {{#submitted}}
            <div class="submitted-box">

                <div class="form-group">
                    <label>제출 제목</label>
                    <div class="submitted-value">{{submitted.resultTitle}}</div>
                </div>

                <div class="form-group">
                    <label>제출 파일</label>
                    <div class="submitted-value">
                        <a href="/download/{{submitted.resultFile}}">{{submitted.resultFile}}</a>
                    </div>
                </div>

                <div class="form-group">
                    <label>제출 내용</label>
                    <div class="submitted-value submitted-contents">
                        {{submitted.resultContents}}
                    </div>
                </div>

                {{#editable}}
                <div class="action-buttons">
                    <a href="/student/assignment/edit/{{submitted.resultNo}}" class="btn-common edit-btn">
                        수정하기
                    </a>
                    <a href="/student/courseOne/{{project.courseNo}}" class="btn-common back-btn">
                        강의 상세로
                    </a>
                </div>
                {{/editable}}

                {{^editable}}
                <div class="action-buttons">
                    <a href="/student/courseOne/{{project.courseNo}}" class="btn-common back-btn">
                        강의 상세로
                    </a>
                </div>
                {{/editable}}
            </div>
            {{/submitted}}

            
            {{^submitted}}
            <form method="post" action="/student/assignment/{{project.projectNo}}" enctype="multipart/form-data">

                <div class="form-group">
                    <label for="resultTitle">제목</label>
                    <input type="text" id="resultTitle" name="resultTitle" placeholder="제목을 입력하세요" required>
                </div>

                <div class="form-group">
                    <label for="submissionFile">파일 첨부</label>
                    <input type="file" id="submissionFile" name="submissionFile" required>
                </div>

                <div class="form-group">
                    <label for="resultContents">내용</label>
                    <textarea id="resultContents" name="resultContents" placeholder="내용을 입력하세요" required></textarea>
                </div>

                <div class="action-buttons">
                    <button type="submit" class="btn-common submit-btn">제출</button>
                    <a href="/student/courseOne/{{project.courseNo}}" class="btn-common back-btn">
                        강의 상세로
                    </a>
                </div>

            </form>
            {{/submitted}}

        </div>

    </div>
</div>

{{> layouts/footer }}

</body>
</html>
