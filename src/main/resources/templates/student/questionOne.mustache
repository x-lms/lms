<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>질문 상세보기</title>
    <link rel="stylesheet" href="/css/common.css">
    <link rel="stylesheet" href="/css/layout.css">
    <link rel="stylesheet" href="/css/questionOne.css">
</head>
<body>
{{> layouts/header }}

<div class="container">
    {{> layouts/navStudent }}

    <div class="main-content">
        <h2 class="page-title">질문 상세보기</h2>

        {{#question}}
        <div class="detail-box">
            <div class="course-info">
                <div><b>강의명:</b> {{courseName}}</div>
                <div><b>제목:</b> {{questionTitle}}</div>
                <div><b>작성일:</b> {{createdate}}</div>
                <div><b>답변 상태:</b> 
                    {{#answered}}답변 완료{{/answered}}
                    {{^answered}}미답변{{/answered}}
                </div>
            </div>

            <div class="section-title">질문 내용</div>
            <div class="question-contents">{{questionContents}}</div>

            {{#answerContents}}
            <div class="section-title">교수 답변</div>
            <div class="answer-contents">{{answerContents}}</div>
            {{/answerContents}}

            {{^answered}}
            <div class="edit-section">
                <!-- 수정 링크 -->
                <a href="/student/questionWrite/{{courseNo}}/{{questionNo}}" class="edit-btn">수정</a>

                <!-- 삭제 폼 -->
                <form method="post" action="/student/questionDelete/{{questionNo}}" style="display:inline;" onsubmit="return confirm('정말 삭제하시겠습니까?');">
                    <button type="submit" class="delete-btn">삭제</button>
                </form>
            </div>
            {{/answered}}
        </div>
        {{/question}}

        {{^question}}
        <div class="error-message">
            <p>질문 정보를 불러올 수 없습니다.</p>
        </div>
        {{/question}}

        <a href="/student/questionList" class="back-btn">목록으로 돌아가기</a>
    </div>
</div>

{{> layouts/footer }}
</body>
</html>
