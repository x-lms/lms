<!DOCTYPE html>

<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>수강신청</title>
    <link rel="stylesheet" href="/css/studentCourse.css">
    <link rel="stylesheet" href="/css/common.css">
    <link rel="stylesheet" href="/css/layout.css">
    <style>
        /* 알림 박스 스타일 */
        .alert-box {
            width: 100%;
            padding: 15px;
            margin-bottom: 20px;
            border-radius: 5px;
            font-weight: bold;
            text-align: center;
        }
        .alert-success {
            background-color: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }
        .alert-error {
            background-color: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }
    </style>
</head>
<body>
    {{> layouts/header }}

<div class="container">
    {{> layouts/navStudent }}


<div class="main-content">
    <div class="page-header">
        <h2 class="page-title">수강신청</h2>
    </div>

    <!-- 메시지 출력 영역 -->
    {{#msg}}
    <div class="alert-box {{#success}}alert-success{{/success}}{{^success}}alert-error{{/success}}">
        {{msg}}
    </div>
    {{/msg}}
	<div class="search-bar">
	    <form action="/student/addCourse" method="get">
	        <input type="text" name="keyword" placeholder="과목명, 교수명 검색">
	        <button type="submit">검색</button>
	    </form>
	</div>

    <table>
        <thead>
            <tr>
                <th>과목번호</th>
                <th>과목명</th>
                <th>교수명</th>
                <th>강의실</th>
                <th>강의요일</th>
                <th>강의시작시간</th>
                <th>강의끝나는시간</th>
                <th>수강인원</th>
                <th>최대인원</th>
                <th>신청</th>
            </tr>
        </thead>
        <tbody>
        {{#courses}}
            <tr>
                <td data-label="과목번호">{{courseNo}}</td>
                <td data-label="과목명">{{courseName}}</td>
                <td data-label="교수명">{{empName}}</td>
                <td data-label="강의실">{{courseLocation}}</td>
                <td data-label="강의요일">{{courseDate}}</td>
                <td data-label="강의시작시간">{{courseTimeStart}}</td>
                <td data-label="강의끝나는시간">{{courseTimeEnd}}</td>
                <td data-label="수강인원">{{currentCnt}}</td>
                <td data-label="최대인원">{{maxCnt}}</td>
                <td data-label="신청">
                    <form action="/student/addCourse/{{courseNo}}" method="post">
                        <button 
                            class="btn"
                            type="submit"
                            {{#isFull}}disabled{{/isFull}}
                        >
                            {{#isFull}}마감{{/isFull}}
                            {{^isFull}}신청{{/isFull}}
                        </button>
                    </form>
                </td>
            </tr>
        {{/courses}}
        </tbody>
    </table>
  <div class="pagination">
    {{#prevPage}}
        <a href="/student/addCourse?page={{.}}{{#keyword}}&keyword={{keyword}}{{/keyword}}">이전</a>
    {{/prevPage}}

    {{#pages}}
        <a href="/student/addCourse?page={{number}}{{#keyword}}&keyword={{keyword}}{{/keyword}}"
           class="{{#isCurrent}}active{{/isCurrent}}">
            {{number}}
        </a>
    {{/pages}}

    {{#nextPage}}
        <a href="/student/addCourse?page={{.}}{{#keyword}}&keyword={{keyword}}{{/keyword}}">다음</a>
    {{/nextPage}}
</div>

   
</div>


</div>

{{> layouts/footer }}


</body>
</html>
